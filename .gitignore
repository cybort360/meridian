# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# circle entity-secret recovery file — NEVER commit
*-recovery-file.json

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files — ignore every .env* except the committed template
.env*
!.env.example

# test artifacts
.playwright-mcp/
login.png

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# KYC document uploads (local dev storage)
/public/uploads

# KYC documents — private, server-only storage (never served statically)
/private-uploads

# Project blueprint — kept local, not published
CLAUDE.md
